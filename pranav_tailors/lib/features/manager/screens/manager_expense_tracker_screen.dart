import 'package:flutter/material.dart';

class ManagerExpenseTrackerScreen extends StatelessWidget {
  const ManagerExpenseTrackerScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Manager Expense Tracker')),
    );
  }
}
