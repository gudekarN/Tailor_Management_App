import 'package:flutter/material.dart';

class ManagerWorkQueueScreen extends StatelessWidget {
  const ManagerWorkQueueScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Manager Work Queue')),
    );
  }
}
