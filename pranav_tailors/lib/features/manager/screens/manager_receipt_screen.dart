import 'package:flutter/material.dart';

class ManagerReceiptScreen extends StatelessWidget {
  const ManagerReceiptScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Manager Receipt')),
    );
  }
}
