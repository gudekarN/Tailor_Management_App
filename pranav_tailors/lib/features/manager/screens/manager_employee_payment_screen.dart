import 'package:flutter/material.dart';

class ManagerEmployeePaymentScreen extends StatelessWidget {
  const ManagerEmployeePaymentScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Manager Employee Payment')),
    );
  }
}
